version: '3'

services:
  mongo_db:
    container_name: database_container3
    image: mongo:latest
    restart: always
    volumes:
      - mongo_db:/data/db
    networks:
      - my-network

  node-app:
    build: .
    ports:
      - "5002:8002"
    environment:
      - PORT=8002
      - DB_URL=mongodb://mongo_db:27017/mono3
      - SALT_ROUND=8
      - TOKEN_SIGNATURE=Loserlolaaa
      - EMAIL_PASSWORD=jqubhqjeatmprplt
      - EMAIL=m.ametwally15@gmail.com
      - EMAIL_SIGNATURE=LolaLoltenLola
    depends_on:
      - mongo_db
    networks:
      - my-network

volumes:
  mongo_db: {}

networks:
  my-network:
    driver: bridge
